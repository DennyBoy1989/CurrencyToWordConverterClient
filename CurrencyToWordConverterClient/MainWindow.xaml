<Window x:Class="CurrencyToWordConverterClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CurrencyToWordConverterClient"
        xmlns:viewmodels="clr-namespace:CurrencyToWordConverterClient.ViewModels" 
        xmlns:uielements="clr-namespace:CurrencyToWordConverterClient.UiElements"
        mc:Ignorable="d"
        Title="Currency-To-Word Converter" Height="450" Width="800">
    <!--
    <Window.DataContext>
        <viewmodels:CurrencyToWordConverterVm/>
    </Window.DataContext>
    -->
    <Grid HorizontalAlignment="Left">
        <Grid HorizontalAlignment="Left">
            <uielements:NumberTextBox HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"  MaxLength="9" Text="{Binding Dollars,Delay=500,UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
        <Grid>
            <uielements:NumberTextBox HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" Margin="400,0,100,0"  MaxLength="2" Text="{Binding Cents,Delay=500,UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
        <Grid>
            <Label Content="{Binding WordRepresentation}" Margin="0,40,0,0" />
        </Grid>
    </Grid>
</Window>
